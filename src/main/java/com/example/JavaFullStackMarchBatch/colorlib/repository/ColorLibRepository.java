package com.example.JavaFullStackMarchBatch.colorlib.repository;

import com.example.JavaFullStackMarchBatch.colorlib.domain.ColorLib;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ColorLibRepository extends JpaRepository<ColorLib,Integer> {
}
